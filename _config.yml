# Site settings
title: Maratona da SI 2024
description: Universidade Federal de Viçosa
domain: lucasnfe.github.io   # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://lucasnfe.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: maratona-ufv            # place folder name if the site is served in a subfolder

lsi: false
safe: true
source: maratona-ufv
incremental: false

# Workshop date
workshop_date: 19 de Outubro de 2024

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

# Custom variables
twitter_username: EXAG_Workshop
youtube_channel: UCXXXXXXXXXXXXXXXXXXXXXXXX

# Collections
collections:
  announcements:
    output: true
  papers:
    output: true

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

# Additional settings
timezone: UTC
encoding: "utf-8"
lang: "en"

